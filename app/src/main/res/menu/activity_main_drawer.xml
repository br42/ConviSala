<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_reservas"
            android:icon="@drawable/ic_menu_camera"
            android:title="@string/menu_reservas" />
        <item
            android:id="@+id/nav_salas"
            android:icon="@drawable/ic_menu_gallery"
            android:title="@string/menu_salas" />
        <!--item
            android:id="@+id/nav_perfil"
            android:icon="@drawable/ic_menu_slideshow"
            android:title="@string/menu_perfil" />
        <item
            android:id="@+id/nav_opcoes"
            android:icon="@drawable/ic_menu_manage"
            android:title="@string/menu_opcoes" /-->
    </group>

    <!--item android:title="@string/communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="@string/menu_share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="@string/menu_send" />
        </menu>
    </item-->

</menu>
